import { Recipe } from '@/types/recipeTypes'
import React from 'react'

type Props = {
    recipe: Recipe
}

const RecipeCards = (props: Props) => {
    return (
        <div>RecipeCards</div>
    )
}

export default RecipeCards